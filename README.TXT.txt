minSdkVersion may need to be changed accordingly, depending on the type of 
device/emulator that the app is being run on. Current
minSdkVersion is set to 21.

Admin code for admin logins is set at "99". 